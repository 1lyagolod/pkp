public class Navigator {
    public double calculateDistance(GPSPoint point1, GPSPoint point2) {
        // Реализация расчета расстояния между двумя точками
        return 0.0;
    }

    public void navigateTo(GPSPoint destination) {
        // Реализация навигации к указанной точке
        System.out.println("Navigating to: " + destination);
    }
}
